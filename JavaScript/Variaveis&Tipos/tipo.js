//exemplo 
// Sem a Tipagem Fraca
let um = "um";

// Para verificar o tipo, digitar o 'typeof' e a variavel
typeof um // resultado vai ser uma String

um = 1
typeof um // reatribuir vai ser um numero.

// Strings

const DIO = 'Digital Innovation One'

let firstName = 'João'
let lastName = 'Luiz'

// feito por interpolação
let fullName = `Nome Completo: ${firstName} ${lastName}`

// String como Objetos

let exemplo = new String("blablabla")

typeof exemplo // retornar um objeto, pois a String, foi declarado em maiscula, dificil String, ser declarada dessa maneira.

// É um dado interável.

nome[1] // busco o indice da String.

// Separar uma frase, utilizando o 'split'
let frase = "Olá, tudo bem?"
frase.split("") // separa letra por letra
frase.split(" ") // separa por palavra

// Uma ideia de validação. com o 'includes'

frase.includes('tudo') // retornar verdadeiro, pois tem 'tudo' na frase.

// Qual letra se inicia.
frase.startsWith('O') // retorna true, pois começa com 'O'

// Para verificar o final
frase.endsWith('?') // final da frase.

// Se utilizar a variável + o ponto, retorna, tudo que tem disponivel

// Number

let num = 100;

// Sobre modulo '%'
num % 2
// resto da divisão, distinguir se é par ou impar.

// metodo 'floor', arredonda para cima.
Math.floor(nomeVariavel)

// metodo 'ceil', arredondar para cima.

Math.ceil(nomeVariavel)

// Porcentagem, declarar o valor que vai mudar em String, trabalha com a concatenar

//Booleans

// Validação de condicionais

let validation = 3 === 0
validation // aparece no console o valor 'false', metodo de comparação.

// Arrays

const array = [] // iniciar uma array

//Objetos

let person = {
    name:'John',
    age:'20'
}

// Empty, undefined e null



